# ESP8266 POC

Build with `cargo build --release` after ESP8266 toolchain and LX106 GCC install.

Only LoLin ESP8266 hardware support claim has recorded board evidence. Record successful `verify-board.sh --port <port> --led-observed` output: `flash=PASS readback=PASS static=PASS serial=PASS timing=PASS led=PASS`.
